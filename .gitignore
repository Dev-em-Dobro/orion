# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/test-results/
/playwright-report/
/blob-report/

# next.js
/.next/
# Builds isolados dos scripts de medição (perf-rotas, shot-tema). Existem pra
# não reescreverem o `.next` que o `next dev` está usando.
/.next-perf/
/.next-shots/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (mantenha .env.example versionado)
.env
.env*.local
.env.development
.env.production

# vercel
.vercel

# typescript
*.tsbuildinfo

# prisma
/prisma/migrations/dev.db*
/prisma/*.db
/prisma/*.db-journal

# editor / IDE
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/

# claude code (config local, per-máquina)
.claude/settings.local.json

# logs
logs
*.log

# python (skills)
__pycache__/
*.pyc

.vercel

# gstack — estado local da ferramenta (logs de browse, cache de disponibilidade).
.gstack/
